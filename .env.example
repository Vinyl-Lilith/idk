Re# Required: set this in Vercel → Project Settings → Environment Variables
# Without this, WebSocket will fall back to localhost and fail in production.
REACT_APP_API_URL=https://backend-and-stuff.onrender.com
